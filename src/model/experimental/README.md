# Experimental modules (not used in paper tables)

These files are exploratory drafts superseded by the versions in `src/model/`.

| File | Status |
|------|--------|
| `analogy_gates.py` | Earlier draft — use `src/model/analogy_gates.py` |
| `magnetopause_geometry.py` | Earlier draft — use `src/model/magnetopause_geometry.py` |
| `spectral_head.py` | Earlier draft — use `src/model/spectral_head.py` |

None of these are referenced by the reported results. All paper runs use `backbone: transformer` and `gate_type: bz`.
